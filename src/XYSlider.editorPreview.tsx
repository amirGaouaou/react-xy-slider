import { Component, ReactNode, createElement } from "react";
import { XYSliderPreviewProps } from "../typings/XYSliderProps";

export class preview extends Component<XYSliderPreviewProps> {
    render(): ReactNode {
        return <div>Preview not done yet</div>;
    }
}
